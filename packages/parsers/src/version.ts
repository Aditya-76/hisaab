/**
 * Stamped into every parsed row so re-parsing after parser upgrades is
 * traceable (docs/TECH-DESIGN.md §3). Bump alongside the package version on
 * any behavior change.
 */
export const PARSER_PACK_VERSION = "0.1.0";
