{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests and their helpers run under Vitest on plain Node (node:sqlite),
    // so Node ambient types are allowed here — and only here.
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": []
}
